apiVersion: v1
kind: Service
metadata:
  name: automotive-app-service
spec:
  type: NodePort
  selector:
    app: automotive-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
      nodePort: 30080
